{
  "name": "fyntra",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm -F api dev\" \"pnpm -F web dev\"",
    "dev:all": "concurrently -n api,web,bridge -c blue,green,magenta \"pnpm -F api dev\" \"pnpm -F web dev\" \"pnpm -F bridge dev\"",
    "dev:web": "pnpm -F web dev",
    "dev:api": "pnpm -F api dev",
    "dev:bridge": "pnpm -F bridge dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
